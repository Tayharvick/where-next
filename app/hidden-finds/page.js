import WhereNext from "@/components/WhereNext";

export const metadata = {
  title: "Hidden Finds — Where Next",
  description: "Editorial Scout Reports on overlooked U.S. towns worth watching before everyone else finds them.",
};

export default function Page() {
  return <WhereNext section="finds" />;
}
