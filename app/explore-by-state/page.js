import WhereNext from "@/components/WhereNext";

export const metadata = {
  title: "Explore by State — Where Next",
  description: "Browse researched U.S. states and towns with honest relocation context from Where Next.",
};

export default function Page() {
  return <WhereNext section="states" />;
}
