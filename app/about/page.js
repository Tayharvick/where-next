import AboutPage from "@/components/AboutPage";

export const metadata = {
  title: "About — Where Next",
  description:
    "Where Next is a relocation scout built to tell you the whole story—housing, trade-offs, risk, and what living somewhere actually means.",
};

export default function Page() {
  return <AboutPage />;
}
